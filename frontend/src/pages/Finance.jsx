import React, { useEffect, useState } from 'react';
import { api } from '../services/api';
export default function Finance(){ const [p,setP]=useState({}); const [m,setM]=useState({}); useEffect(()=>{api('/finance/profit-loss').then(setP); api('/finance/monthly-summary').then(setM)},[]); return <div className="grid"><div className="card"><h3>Income</h3><div className="metric">₹{Number(p.income||0).toLocaleString('en-IN')}</div></div><div className="card"><h3>Expenses</h3><div className="metric">₹{Number(p.expense||m.expenses||0).toLocaleString('en-IN')}</div></div><div className="card"><h3>Profit</h3><div className="metric">₹{Number(p.profit||0).toLocaleString('en-IN')}</div></div><div className="card"><h3>Monthly Sales Count</h3><div className="metric">{m.saleCount||0}</div></div></div> }

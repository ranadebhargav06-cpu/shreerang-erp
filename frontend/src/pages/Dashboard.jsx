import React, { useEffect, useState } from 'react';
import { LineChart, Line, ResponsiveContainer, XAxis, YAxis, Tooltip, BarChart, Bar } from 'recharts';
import { api } from '../services/api';
const rupee = v => `₹${Number(v||0).toLocaleString('en-IN')}`;
export default function Dashboard(){ const [d,setD]=useState(null); useEffect(()=>{api('/dashboard').then(setD).catch(()=>setD({}))},[]); if(!d) return <div className="card">Loading dashboard...</div>; return <><div className="grid"><div className="card"><h3>Today's Sales</h3><div className="metric">{rupee(d.todaysSales)}</div></div><div className="card"><h3>Monthly Sales</h3><div className="metric">{rupee(d.monthlySales)}</div></div><div className="card"><h3>Outstanding</h3><div className="metric">{rupee(d.outstandingPayments)}</div></div><div className="card"><h3>Customers</h3><div className="metric">{d.totalCustomers||0}</div></div></div><div className="module-grid" style={{marginTop:16}}><div className="card"><h3>Revenue Analytics</h3><div className="chart"><ResponsiveContainer><LineChart data={d.revenueByDay||[]}><XAxis dataKey="date"/><YAxis/><Tooltip/><Line dataKey="revenue" stroke="#9a3412" strokeWidth={3}/></LineChart></ResponsiveContainer></div></div><div className="card"><h3>Top Selling Products</h3><div className="chart"><ResponsiveContainer><BarChart data={d.topSellingProducts||[]}><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey="quantity" fill="#f97316"/></BarChart></ResponsiveContainer></div></div></div></> }

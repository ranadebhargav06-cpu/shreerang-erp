import React, { useState } from 'react';
import { useAuth } from '../contexts/AuthContext';
export default function Login(){ const { login }=useAuth(); const [email,setEmail]=useState('admin@shreerangpapad.com'); const [password,setPassword]=useState('admin123'); const [error,setError]=useState(''); return <div className="login-page"><div className="login-card"><div className="brand"><div className="brand-mark">SP</div><div><h1>Shreerang Papad ERP</h1><p className="muted">Mahagaon, Yavatmal, Maharashtra</p></div></div><form className="form" onSubmit={async e=>{e.preventDefault();setError('');try{await login(email,password)}catch(err){setError(err.message)}}}><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/>{error&&<div className="error">{error}</div>}<button>Login</button><p className="muted">Default seed login: admin@shreerangpapad.com / admin123</p></form></div></div> }
